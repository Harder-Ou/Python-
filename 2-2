#本题目要求计算下列分段函数f(x)的值：
#f(x)=0  ,x=0
#f(x)=1/x ,x!=0
#输入格式:
#输入在一行中给出实数x。
#输出格式:
#在一行中按“f(x) = result”的格式输出，其中x与result都保留一位小数。

#解法1
import math
x = float(input())
if x == 0.0:
    result = x
else:
    result = 1/x
print('f(%.1f) = %.1f'%(x,result))

#解法2
x=input()
x=float(x)
if x==0:
    print('f(%.1f) = %.1f'%(x,x))
else:
    print('f(%.1f) = %.1f' %(x,1/x))
    
#解法3
x=input()
x=float(x)
if x==0:
    print('f({0}) = {1}'.format(x, x))
else:
    print('f({0}) = {1:.1f}'.format(x, 1/x))
